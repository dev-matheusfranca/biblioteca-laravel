@csrf
<div class="field"><label for="name">Nome</label><input id="name" name="name" class="form-control" value="{{ old('name', $leitor->name ?? '') }}" maxlength="255" required autocomplete="name" @error('name') aria-invalid="true" aria-describedby="name-error" @enderror>@error('name')<span id="name-error" class="field-error">{{ $message }}</span>@enderror</div>
<div class="field"><label for="email">E-mail</label><input id="email" name="email" type="email" class="form-control" value="{{ old('email', $leitor->email ?? '') }}" maxlength="255" required autocomplete="email" @error('email') aria-invalid="true" aria-describedby="email-error" @enderror>@error('email')<span id="email-error" class="field-error">{{ $message }}</span>@enderror</div>
<div class="field"><label for="password">{{ isset($leitor) ? 'Nova senha (opcional)' : 'Senha inicial (opcional)' }}</label><input id="password" name="password" type="password" class="form-control" minlength="8" autocomplete="new-password" aria-describedby="password-help @error('password') password-error @enderror" @error('password') aria-invalid="true" @enderror><span id="password-help" class="field-hint">Use ao menos 8 caracteres. {{ isset($leitor) ? 'Deixe em branco para manter a senha atual.' : 'Sem senha inicial, o leitor deverá usar “Esqueci minha senha” para definir o acesso.' }}</span>@error('password')<span id="password-error" class="field-error">{{ $message }}</span>@enderror</div>
<div class="field"><label for="password_confirmation">Confirme a senha</label><input id="password_confirmation" name="password_confirmation" type="password" class="form-control" minlength="8" autocomplete="new-password"></div>
<input type="hidden" name="is_active" value="0"><label class="checkbox-field" for="is_active"><input id="is_active" name="is_active" type="checkbox" value="1" @checked(old('is_active', $leitor->is_active ?? true))> Acesso ativo</label>
<p class="muted">A inativação bloqueia o acesso e novos empréstimos, preservando o histórico. Nenhum convite é enviado automaticamente.</p>
<div class="form-actions"><button class="btn btn-primary" type="submit">Salvar leitor</button><a class="btn btn-ghost" href="{{ route('leitores.index') }}">Cancelar</a></div>
